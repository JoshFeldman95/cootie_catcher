- add map
- add actions to map
- add randomize button
- add clear all button
- add epi curves 
- indicate last test


user action -> state update -> draw

update:
    check for action

    if action:
        event.fire()

event.fire:
    update state